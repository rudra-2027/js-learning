"use strict"; // treate all JS code as newer version
// alert("Hello") // we can use directly from here we are using nodejs, not browser
// console.log(3+3) console.log("Rudra") // Problem is code readbility  which give error
console.log(3
    +
    3) // code readability should be high
// console.log(alert("Hello World"))+


let name = "hitesh" //string
let age = 18 //int
let isLoggedIn = false //boolean
let temp = null // example if our site wants to fetch a temperature  by mistake there is system failure so it will return null not a 0 because 0 is a temprature

// numbeer=> 2 power 53
// bigint => for larger numbers
// string => ""
// boolean => true/false;
// null => standalone value(type = object)
// undefined => value is not assigned
// symbol => very unique? to use for find the uniquenese 


// object 
console.log(typeof "Histesh") // type is String
console.log(typeof age);
console.log(typeof null)
console.log(typeof undefined)
